<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="JustHostMC.App.Controls.LogViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <DataTemplate x:Key="DefaultLogLineTemplate">
            <TextBlock
                FontFamily="Consolas"
                FontSize="12"
                IsTextSelectionEnabled="True"
                Text="{Binding}"
                TextWrapping="Wrap" />
        </DataTemplate>
    </UserControl.Resources>

    <Border
        Background="{x:Bind Background, Mode=OneWay}"
        BorderBrush="{x:Bind BorderBrush, Mode=OneWay}"
        BorderThickness="{x:Bind BorderThickness, Mode=OneWay}"
        CornerRadius="{x:Bind CornerRadius, Mode=OneWay}">
        <Grid>
            <ListView
                x:Name="LogList"
                Padding="{x:Bind Padding, Mode=OneWay}"
                Background="Transparent"
                ItemsSource="{x:Bind ItemsSource, Mode=OneWay}"
                SelectionMode="None">
                <ListView.ItemContainerTransitions>
                    <TransitionCollection />
                </ListView.ItemContainerTransitions>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="MinHeight" Value="0" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="IsTabStop" Value="False" />
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>

            <ContentPresenter
                x:Name="EmptyPresenter"
                Margin="24"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Content="{x:Bind EmptyContent, Mode=OneWay}"
                Visibility="Collapsed" />

            <Button
                x:Name="JumpToLatestButton"
                x:Uid="LogViewerJumpToLatestButton"
                Width="36"
                Height="36"
                MinWidth="36"
                MinHeight="36"
                Margin="12"
                Padding="0"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Click="OnJumpToLatestClick"
                Visibility="Collapsed">
                <FontIcon FontSize="14" Glyph="&#xE70D;" />
            </Button>
        </Grid>
    </Border>
</UserControl>
